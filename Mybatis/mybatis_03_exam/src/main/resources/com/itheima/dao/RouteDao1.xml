<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.dao.RouteDao1">
    <!-- public Seller findByRid(int sid);-->
    <select id="findByRid" resultType="Seller" parameterType="int">
        select * from tab_seller where sid=#{sid}
    </select>
</mapper>